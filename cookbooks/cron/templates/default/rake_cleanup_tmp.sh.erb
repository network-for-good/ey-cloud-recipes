#!/bin/bash

cd <%= @app_dir %>
logger "Cron: Cleaning up /tmp/ directory"
su -c "RAILS_ENV=<%= @environment %> /usr/local/bin/bundle exec rake cleanup:tmp" deploy
find /tmp/* -type f -mtime +7 -print -delete
find /tmp/* -type d -mtime +7 -print -delete