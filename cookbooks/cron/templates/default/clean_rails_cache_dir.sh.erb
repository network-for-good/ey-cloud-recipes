#!/bin/sh

APP_DIR=<%= @app_dir %>
ROO_DIR=<%= @roo_dir %>

dirs=($APP_DIR $ROO_DIR)
for d in $dirs; do
  if [ -d "${d:-NULL}" ]; then
    find $d -type f -mtime +7 -not -path "*/assets/*" -print -delete
  fi
done
